#' @keywords internal
#' @description
#'
#' Exported Functions:
#' * `ombc_gmm` - Identify outliers while clustering the data by fitting a
#'                Gaussian mixture model.
#' * `ombc_lcwm` - Identify outliers while performing regression and clustering
#'                 by fitting a linear cluster-weighted model to the data.
#' * `simulate_gmm` - Simulate data from a Gaussian mixture model with outliers.
#' * `simulate_lcwm` - Simulate data from a linear cluster-weighted model with
#'                     outliers.
#' * `find_gross` -
#' * `backtrack` -
#' * `backtrack_gmm` -
#' * `backtrack_lcwm` -
#' * `plot_curve` -
#' * `plot_backtrack` -
"_PACKAGE"

## usethis namespace: start
## usethis namespace: end
NULL
